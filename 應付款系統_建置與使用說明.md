# 程優教育科技｜應付款指揮中心 · 建置與使用說明

> 這套系統解決的問題：助理回報一筆應付款後，主管太忙、訊息一多就忘記處理，
> 事後說不清楚「到底有沒有講過」，結果變成違約金甚至斷電停工。
> 系統會把每一筆款項的「回報 → 確認 → 處理 → 完成」全程留下時間戳記，
> 助理可以隨時「再提醒」，主管的儀表板會清楚看到「還有幾筆沒確認」，
> 準時處理有積分、有連續零逾期天數可以累積，逾期則會歸零。

---

## 一、這次交付的檔案

| 檔案 | 用途 |
|---|---|
| `應付款系統_GoogleAppsScript.gs` | 後端＋資料庫（要貼進 Apps Script 專案的 `Code.gs`） |
| `應付款系統_Index.html` | 前端畫面（要貼進 Apps Script 專案裡一個檔名為 `Index` 的 HTML 檔） |
| `應付款系統_appsscript.json` | 專案設定檔（讓系統能認出訪問者的 Google 帳號） |
| 本說明文件 | 建置步驟＋日常操作方式 |

**架構就一句話**：整個系統（畫面＋資料庫）全部住在一個 Google Apps Script 專案裡，
不用另外租主機、不用另外設定登入系統——3 個人打開同一個網址，Apps Script
會自動用「你當下登入 Google 的帳號」認出你是誰，比對試算表裡的白名單決定
你是主管還是助理。資料就存在一份 Google 試算表裡，跟你們現有的「主管人才
評比系統」用的是同一套邏輯，一切從簡。

---

## 二、建置步驟（鄭董操作，約 10–15 分鐘，只需做一次）

### 步驟 1：建立 Apps Script 專案
1. 打開 [script.google.com](https://script.google.com) → 「新增專案」
2. 把 `應付款系統_GoogleAppsScript.gs` 整份內容貼上，取代預設的 `Code.gs` → 存檔

### 步驟 2：加入前端畫面檔
1. 左側檔案列表旁「＋」→「HTML」
2. **檔名務必打「Index」**（不要加 `.html`，Apps Script 會自動處理）
3. 把 `應付款系統_Index.html` 整份內容貼進去 → 存檔

### 步驟 3：設定專案權限清單
1. 左側齒輪圖示「專案設定」→ 勾選「在編輯器中顯示 `appsscript.json` 資訊清單檔案」
2. 回到編輯器，打開 `appsscript.json` → 整份換成 `應付款系統_appsscript.json` 的內容 → 存檔

### 步驟 4：執行一鍵建置
1. 上方函式下拉選單選「`buildSystem`」→ 按「執行 ▶」
2. 第一次執行會跳出授權畫面 → 選鄭董的 Google 帳號 → 允許
   （會看到「Google 尚未驗證這個應用程式」是正常的，點「進階」→「前往...（不安全）」即可，
   這只是 Google 對自架小工具的標準警語，不影響安全性）
3. 執行完成後，點上方「執行紀錄／Executions」，會看到剛建立的試算表連結

### 步驟 5：分享資料給淑靜、Alley（很重要，不做這步他們會打不開）
系統是用「訪問者自己的身分」讀寫試算表，所以試算表跟附件資料夾都要分享給他們：
1. 打開步驟 4 產生的試算表 → 右上角「共用」→ 加入淑靜、Alley 的 Gmail，權限選「編輯者」
2. 打開 Apps Script 執行紀錄裡的「附件資料夾」連結 → 同樣「共用」給他們兩位「編輯者」權限

### 步驟 6：填入白名單
打開試算表「白名單」分頁，把淑靜、Alley 的**真實 Gmail** 填進 B、C 欄
（A 欄鄭董的信箱已經自動帶入）。這個分頁就是系統認人的依據，日後要加新的人，
直接在這裡多加一列即可。

### 步驟 7：發布網頁應用程式
1. 回到 Apps Script 編輯器右上角「部署」→「新增部署作業」
2. 齒輪選「網頁應用程式」
3. 設定：
   - 說明：隨便打（例如 `v1`）
   - 執行身分：**存取網頁應用程式的使用者**
   - 誰可以存取：**知道這個 Google 帳戶的任何人**
4. 按「部署」→ 會出現一個「網頁應用程式」網址，**這就是 3 個人都要用的網址**，
   收藏起來或加到手機主畫面即可

> 之後如果修改了程式碼，記得回到「部署」→「管理部署作業」→ 編輯（鉛筆圖示）→
> 版本選「新版本」→ 部署，網址才會更新內容，單純存檔不會自動生效。

---

## 三、日常操作

### 助理端（淑靜、Alley）
- 打開網址（建議加到手機主畫面，跟開 App 一樣方便）
- 右下角「＋」新增應付款：可以直接**拖曳帳單照片、貼上截圖／文字、拍照上傳、或按麥克風用講的**，
  系統會試著自動抓金額跟到期日，抓錯了自己再改一下即可
- 送出後，項目會出現在主管畫面「待確認」欄，主管按下「確認已讀」之前，
  你可以隨時按「🔔 再提醒主管」——每次提醒都會留下時間紀錄，這就是「我真的有講過」的證據
- 常態性的款項（房租、水電）可以在項目詳情頁按「⧉ 複製建立新一筆」，
  直接帶入上次的廠商／金額，只要改到期日

### 主管端（鄭董）
- 儀表板一打開就看得到「待確認」有幾筆、有沒有標紅的緊急項目
- 按「✓ 確認已讀」代表「我已經知道這件事了」，項目會移到「待處理」
- 「▶ 開始處理」→「✔ 標記完成」，準時完成會拿到積分、連續零逾期天數 +1；
  逾期才完成，天數會歸零（這是刻意設計的，逼自己不要拖）
- 頂部「預覽助理視角」可以切換看助理端畫面長怎樣，不影響實際權限

### 大家都看得到
- 每筆項目點下去，右側會展開完整時間軸（誰在何時建立、催辦、確認、完成），
  這份紀錄公開透明，就是為了解決「有沒有講過」的爭議
- 右上角積分排行榜、連續零逾期天數，鼓勵大家準時處理

---

## 四、目前版本的已知限制（誠實說明，之後可以再加強）

- **語音輸入**：用瀏覽器內建的語音辨識（Chrome 效果最好），不需要額外付費服務，
  但少數瀏覽器／環境可能無法要求到麥克風權限，屆時系統會提示改用其他輸入方式，不影響其他功能
- **照片／檔案附件**：目前只會把檔案存起來、附加在項目上，**還沒有自動辨識照片裡的金額**
  （沒有串接 OCR 辨識服務）。文字貼上跟語音輸入這兩種「自動抓金額/日期」是真的有在運作的
- **提醒方式**：目前只有系統內的視覺化提醒（紅點、待辦清單、催辦次數），沒有另外串 LINE
  或 Email 通知，跟您先前選的方向一致；如果之後想加，我可以再協助設定 LINE Notify
- 3 個人共用同一份試算表資料，畫面每 20 秒自動同步一次，也可以按右上角「⟳」立即同步

---

## 五、如果卡關了

- 打開網頁後卡在「連線中…」不動：通常是步驟 7 的部署設定沒選對（執行身分要選
  「存取網頁應用程式的使用者」），或是還沒完成步驟 5 的共用
- 出現「此 Google 帳號尚未加入白名單」：畫面上會顯示目前登入的信箱，把它填進
  試算表「白名單」分頁即可，不用重新部署
- 助理打開後一片空白或跳出權限錯誤：通常是忘記把試算表／附件資料夾分享給他，
  回到步驟 5 檢查
- 改了程式碼但畫面沒變：記得「部署 → 管理部署作業 → 編輯 → 新版本」

有任何步驟卡住，把錯誤訊息或截圖傳給我，我再協助排查。

---
*程優教育科技股份有限公司 · 應付款指揮中心 v1.0*
